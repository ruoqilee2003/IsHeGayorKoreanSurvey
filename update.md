# 問卷更新規格 v2

**論文題目**：Is he Gay or Korean？淺析 KPOP 男性偶像團體的 RPS 吸引力因子與解讀實踐——以 SEVENTEEN 為例

---

## 一、與 v1 的主要差異

| 項目 | v1 | v2 | 理由 |
|---|---|---|---|
| 片段題 | 三則，放在因子題之前 | 一則，移到最末 | 前置片段會預先訓練填答者用「曖昧」框架思考，系統性高估詮釋空間構面 |
| 分組 | 克拉 / 非克拉 | KPOP / 非 KPOP | 研究問題三問的是 KPOP 脈絡，非克拉的 KPOP 粉絲不應被排除 |
| 克拉題 | 無 | 新增專屬區塊 | 為 SEVENTEEN 案例分析提供深度資料 |
| 後台入口 | 首頁可見按鈕 | `#admin` 隱藏路由 | 前台不應出現研究者介面 |

---

## 二、分流邏輯（兩層獨立）

```
第一層｜決定因子題用語版本
  Q2 = 有，3 年以上 / 有，但時間還不長  →  version = "KPOP"
  Q2 = 曾經追過，現在沒有 / 從來沒有      →  version = "OTHER"

第二層｜決定是否顯示克拉專屬區塊
  Q2b 勾選含 SEVENTEEN  →  is_carat = true
  否則                    →  跳過第四部分
```

兩層互相獨立。非克拉的 KPOP 粉絲拿到 KPOP 版因子題，但不顯示第四部分。

---

## 三、頁面順序

```
0  封面
1  第一部分：關於你          Q1 Q2 Q2b Q3 Q4
2  第二部分：你嗑的配對      CP / 八項因子 / 跨圈共通 / 嗑不下去
3  第三部分：兩題獨立題      real / acc / why
4  第四部分：克拉專屬        僅 is_carat = true 顯示
5  第五部分：一則片段        amb / day / open
6  完成頁
```

進度條分母需依 `is_carat` 動態調整（6 或 5）。

---

## 四、題目全文

### 封面

> 這是一份為同人研討會做的意見徵集，完全匿名，大約 5 分鐘。
> 沒有正確答案，只想知道你怎麼看。
> 結果僅用於發表，可能會引用你寫的文字，但不會出現任何身分資訊。按下開始即視為同意。

---

### 第一部分：關於你

**Q1**（必填，單選）你平常會「嗑 CP」嗎？
- 會，這是我主要的興趣
- 偶爾會，但不算深入
- 幾乎不會
- 完全不會

**Q2**（必填，單選）你目前有在追 KPOP 偶像團體嗎？
- 有，3 年以上
- 有，但時間還不長
- 曾經追過，現在沒有
- 從來沒有

**Q2b**（選填，複選）*僅當 Q2 為前兩項時顯示*
你目前主要追的是哪些團？
- SEVENTEEN
- 其他男團
- 女團
- 其他（文字輸入）

**Q3**（必填，單選）你對韓國影視、綜藝或韓語的接觸程度？
- 懂韓語，或長期接觸
- 看過一些，大致熟悉
- 幾乎沒接觸

**Q4**（必填，單選）你聽過「Is he gay or European／Korean」這個梗嗎？
- 聽過
- 沒聽過

---

### 第二部分：你嗑的配對

導言（依版本切換）：
- KPOP 版：「接下來想問問你目前主要嗑的配對。」
- OTHER 版：「接下來想問問你目前主要嗑的配對，可以是任何領域。」

**Q5**（選填，複選）*僅 OTHER 版顯示*
你主要嗑的 CP 來自哪些領域？
日本作品（動漫、遊戲、聲優）／歐美影視／華語圈／文學或舞台／其他

**Q6**（選填，文字）你目前最主要嗑的是哪一對？
提示：可以寫多對，也可以留白。

**Q7**（必填，複選上限 3，附排序）這對最吸引你的地方是什麼？
提示：最多選三項。點選的順序就是優先順序，再點一次可以取消。

| # | KPOP 版 | OTHER 版 | 說明（兩版共用，僅括號內容替換） |
|---|---|---|---|
| 1 | 反差 | 反差 | 身高、年齡、性格或氣質上的落差 |
| 2 | 關係位置 | 關係位置 | KPOP：형／동생 的年齡順序、擔當分工、隊長與隊員之間的不對等<br>OTHER：年齡或資歷的先後、能力上的分工、職位或立場的不對等 |
| 3 | 未解決的張力 | 未解決的張力 | 曖昧沒挑明，或疏遠過又靠近 |
| 4 | 團隊情誼 | 共同經歷 | 一起走過來的時間，累積出的關係厚度 |
| 5 | 視覺配對感 | 視覺配對感 | 單純就是好看，同框畫面本身就成立 |
| 6 | 官方推力 | 官方推力 | KPOP：同 unit、同房、固定搭檔、專屬企劃<br>OTHER：原作或官方一直把他們放在一起、給固定搭檔或專屬篇章 |
| 7 | 可否認性 | 可否認性 | 不用被定義成什麼關係，怎麼想都可以 |
| 8 | 圈子誘因 | 圈子誘因 | 糧多、周邊多、社群熱鬧，可以一路考古挖下去 |

**Q8**（選填，文字）其他（如果上面都不夠貼切）

**Q9**（選填，長文字）
- KPOP 版：你在別的圈嗑過的 CP，跟這對有什麼共同點嗎？
- OTHER 版：你嗑過的幾對 CP 之間，有什麼共同點嗎？

**Q10**（選填，長文字）有沒有哪一對是你「知道很多人嗑但自己嗑不下去」的？為什麼？

頁尾保留：手動切換版本連結、「我沒有在嗑 CP，跳過這部分」。

---

### 第三部分：關於「是不是安排好的」

**Q11**（必填，單選）對你來說，一個互動「看起來不像安排好的」——例如鏡頭外的片刻、不經意的畫面——會影響它的吸引力嗎？
- 影響很大，越不像演的越有價值
- 有影響，但不是最重要的
- 不太影響，是不是安排的都一樣好嗑
- 我反而喜歡明顯是設計過的橋段

**Q12**（必填，單選）當你發現他們似乎知道粉絲在嗑，並且配合演出時，你的感覺比較接近？
- 更喜歡了，有一種共犯感
- 有點掃興，會覺得變成商品
- 沒差，本來就知道是這樣
- 沒想過這件事

**Q13**（選填，長文字）承上，可以說說為什麼嗎？

> Q11 與 Q12 邏輯上互斥。兩題皆選第一項者，即為 Busse 所述雙重意識之直接展現。分析時做 2×2 交叉表。

---

### 第四部分：只問克拉（`is_carat = true` 才顯示）

導言：「接下來幾題只問克拉，想多聽一點細節。」

**Q14**（選填，長文字）你入坑這對是從哪個瞬間開始的？

**Q15**（選填，單選）同 unit 和跨 unit 的配對，哪一種對你來說更好嗑？
- 同 unit，因為互動多、有共同工作的基礎
- 跨 unit，因為同框少、每次都珍貴
- 沒差，跟 unit 沒什麼關係
- 沒想過這件事

**Q16**（選填，單選）出道十年這件事，對你嗑的這對來說重要嗎？
- 很重要，時間長度本身就是吸引力
- 有影響，但不是主要的
- 不太重要
- 沒想過這件事

**Q17**（選填，長文字）十三人裡，有沒有哪一對是你覺得「大家都在嗑但我嗑不起來」的？可以說說為什麼嗎？

> 本區塊不進跨組比較，資料用於論文第五章第（四）節與簡報引言。

---

### 第五部分：一則片段

導言：「最後一題。以下是一段男性偶像團體成員之間的互動，成員以 A、B 代稱。請憑直覺回答。」

> **片段**
> 直播即將結束時，A 對著鏡頭旁的 B 說「사랑해」（我愛你），B 隨口回了「나도」（我也是）。兩人隨即結束直播。

**Q18**（必填，7 點量表）這個互動看起來有多曖昧？
1 完全不曖昧 —— 7 非常曖昧

**Q19**（必填，7 點量表）這個互動有多「日常」？在一般同性朋友之間有多常見？
1 很不尋常 —— 7 非常常見

保留孔版疊印回饋元件。

**Q20**（必填，長文字）你看到的是什麼？請用你自己的話描述，不限字數。

**Q21**（選填，長文字）如果哪天官方明確定義了他們的關係——無論定義成什麼——你的感覺會如何？

> 選這一則是因為它文化解碼落差最大，最直接測試主標題。Q21 以反面測「可否認性」，因填答者不會自發將「不必被定義」辨識為吸引力。

---

## 五、資料欄位

```js
{
  submitted_at, version, is_carat, skipped,
  q1, q2, q2b[], q2b_other, q3, q4,
  fields[], cp, factors[], fac_other, cross, reject,
  real, acc, why,
  carat_enter, carat_unit, carat_years, carat_reject,
  amb, day, open, define
}
```

CSV 欄位順序：

```
submitted_at, version, is_carat, q1, q2, q2b, q3, q4,
cp, fields, factor1, factor2, factor3, fac_other, cross, reject,
real, acc, why,
carat_enter, carat_unit, carat_years, carat_reject,
amb, day, open, define, skipped
```

`factors[]` 拆成三欄，陣列欄位以 ` / ` 串接。CSV 前置 BOM（`\uFEFF`）。

---

## 六、後台改為 `#admin`

靜態頁面沒有伺服器路由，`/admin` 無法直接實作，改用 hash 路由，效果相同：分享 `你的網址#admin` 才會出現後台，一般填答者的網址不含 hash，完全看不到。

**要改的三處：**

1. 刪掉 `#adminZone` 整塊 HTML 裡的 `<button class="linky" id="adminToggle">研究者專用</button>`，只留 `#adminPanel`。

2. 新增判斷函式，並在 `render()` 與 `hashchange` 時呼叫：

```js
function isAdmin(){ return location.hash === "#admin"; }

function syncAdmin(){
  const on = isAdmin();
  document.querySelector("#adminZone").classList.toggle("hidden", !on);
  if(on && !adminLoaded){ adminLoaded = true; refreshCount(); }
}
let adminLoaded = false;
window.addEventListener("hashchange", syncAdmin);
```

3. 在 `render()` 裡，把原本這行

```js
$("#adminZone").classList.toggle("hidden", step !== 0);
```

改成

```js
syncAdmin();
```

`submitAll()` 結尾原本的 `$("#adminZone").classList.remove("hidden")` 一併改成 `syncAdmin()`，否則送出後後台會對所有人跳出來。

**注意**：hash 只是隱藏入口，不是權限控制。共享儲存的資料任何填答者理論上都有機會讀到，所以問卷全程匿名是必要條件而非選項。這一點建議也寫進論文的研究倫理段落。